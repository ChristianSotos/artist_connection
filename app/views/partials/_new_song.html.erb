<div id='new-song-div'>
	<span class='close'>X</span>
	<h1>Song Information</h1>
	<% if flash[:errors]
		flash[:errors].each{|e|%>
		<p><%=e%></p>
		<% }
	end %>
	<%= form_for @song, :url => '/songs/create', :html => {:multipart => true, :class => "no-file"} do |f| %>
		<p><%= f.label :name %>
		<%= f.text_field :name %></p>
		<p><label for='song[bpm]'>BPM</label>
		<%= f.text_field :bpm %></p>
		<p><label for='song[key]'>Key Signature</label>
		<%= f.text_field :key %></p>
		<p><label for='song[public]'>Availibility</label>
			<select name='song[public]'>
				<option value='true'>Public</option>
				<option value='false'>Private</option>
			</select>
		</p>
		<p><label for='song[genre]'>Genre</label>
			<select name='song[genre]'>
			<%	@genres.each{|g| %>
					<option value="<%=g.id%>"><%=g.name%></option>
			<% } %>
			</select>
		</p>
		<% if session[:song_qty] > 1 %>
			<button>Add Next Song</button>
		<% else %>
			<button>Add Song</button>
		<% end %>
	<% end %>
</div>